services:
  fastapi_app:
    container_name: tailtales_cont
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000"
    env_file:
      - .env
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
